with Ada.Strings.Unbounded; use Ada.Strings.Unbounded;
with Ada.Text_IO; use Ada.Text_IO;
with Movil; use Movil;
procedure Main is
   --Generar la clase Movil con los siguientes atributos
   --PosicionX, PosicionY, VelocidadX, VelocidadY
   --Los objetos de esa clase se incializan en el (0,0)
   --con una velocidadX e Y que
   --debe ser definida al momento de instanciar un objeto
   --Agregar un metodo
   ----To_String() que muestra la posicion actual del movil
   --Agregar un metodo
   ----Actualizar que recibe un delta de tiempo
   --y actualiza la posicion del movil

   Movil_Prueba : Vehiculo;
begin


end Main;
